{
  "name": "attention-press",
  "version": "0.1.0",
  "private": true,
  "description": "Reader-funded pay-per-second publishing on Monad",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:contracts": "npm test -w @attention-press/contracts",
    "test:sdk": "npm test -w @attention-press/reader-sdk"
  }
}
