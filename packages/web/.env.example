# Copy to .env.local. All must be NEXT_PUBLIC_ to reach the browser.

# Monad testnet
NEXT_PUBLIC_CHAIN_ID=10143
NEXT_PUBLIC_RPC_URL=https://testnet-rpc.monad.xyz

# Deployed contracts (see packages/contracts/deployments/monadTestnet.json)
NEXT_PUBLIC_ARTICLE_REGISTRY=0x34C48D04c566131aEa6DBA8E2727423A55e38aaa
NEXT_PUBLIC_ATTENTION_STREAM=0x29f111C6eadbe298865b8bF814d5fdafDB47A0AA
# like / dislike / favorite / reply / tip. Defaults in code to the deployed
# testnet address; override here after a redeploy.
NEXT_PUBLIC_ARTICLE_ACTIONS=0x42F229857be71a239393F0eE218aE2A0823FaCa5

# Article floated to the top of discovery (the project explainer). Defaults to 14
# in code; override or set to 0 to disable.
NEXT_PUBLIC_PINNED_ARTICLE_ID=14

# Where the reader SDK POSTs signed vouchers
NEXT_PUBLIC_COLLECTOR_URL=http://localhost:8787

# Pay-rate tiers are defined in src/lib/chain.ts (RATE_TIERS) and chosen per
# article at publish time; the session budget is derived (rate × 5 min).
